import React from "react";

export const metadata = {
  title: "Lokafly - About us",
};

const AboutLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default AboutLayout;
